# W2 — Ideas Stage Split (N-PAGE-IMPL-1)\n+\n+Split the hybrid Ideas page into:\n+- `/planning/posts/<id>/calendar/ideas/generate` → RUN_PAGE (ideas.generate_idea_set)\n+- `/planning/posts/<id>/calendar/ideas/curate` → EDIT_PAGE (ideas.curate_ideas)\n+\n+Preserve `/planning/posts/<id>/calendar/ideas` as a redirect to the Generate page. No gating logic changes.\n+\n+---\n+\n+## 1. Routes Implemented\n+\n+### 1.1 New routes\n+\n+In `blueprints/planning.py`:\n+\n+```python\n+@bp.route('/posts/<int:post_id>/calendar/ideas/generate')\n+def planning_calendar_ideas_generate(post_id):\n+    \"\"\"Ideas Generate page (RUN_PAGE: ideas.generate_idea_set).\"\"\"\n+    return _render_ideas_page(post_id, page_mode=\"generate\")\n+\n+\n+@bp.route('/posts/<int:post_id>/calendar/ideas/curate')\n+def planning_calendar_ideas_curate(post_id):\n+    \"\"\"Ideas Curate page (EDIT_PAGE: ideas.curate_ideas).\"\"\"\n+    return _render_ideas_page(post_id, page_mode=\"curate\")\n+```\n+\n+Helper (same DB logic as `planning_calendar_clean.planning_calendar_ideas`, but exposes `page_mode` to the template):\n+\n+```python\n+def _render_ideas_page(post_id: int, page_mode: str):\n+    \"\"\"Shared renderer for post-based ideas pages.\"\"\"\n+    try:\n+        from flask import request, redirect, url_for\n+        from datetime import datetime\n+        from utils.taxonomy_helpers import get_post_type\n+\n+        post_type = get_post_type(post_id)\n+        if post_type == 'recipe':\n+            return redirect(url_for('authoring.authoring_sections_drafting', post_id=post_id))\n+        elif post_type == 'generated':\n+            return redirect(url_for('planning.planning_calendar_taxonomy', post_id=post_id))\n+\n+        url_year = request.args.get('year', type=int)\n+        url_week = request.args.get('week', type=int)\n+        if url_year and url_week:\n+            year = url_year\n+            week_number = url_week\n+        else:\n+            now = datetime.now()\n+            year = now.year\n+            week_number = now.isocalendar()[1]\n+\n+        target_post_id = post_id\n+        content_type_name = None\n+        with db_manager.get_cursor() as cursor:\n+            cursor.execute(\"\"\"\n+                SELECT p.id, p.title, p.status, p.summary, p.subtitle, p.created_at, p.updated_at,\n+                       p.content_type_id\n+                FROM post p\n+                WHERE p.id = %s\n+            \"\"\", (target_post_id,))\n+            post = cursor.fetchone()\n+\n+            if not post:\n+                return render_template(\n+                    'planning/calendar/ideas.html',\n+                    post_id=post_id,\n+                    year=year,\n+                    week_number=week_number,\n+                    blueprint_name='planning',\n+                    mode='post-based',\n+                    page_mode=page_mode,\n+                    error='Post not found',\n+                )\n+\n+            cursor.execute(\"\"\"\n+                SELECT ti.display_name as content_type_name\n+                FROM post p\n+                LEFT JOIN taxonomy_item ti ON p.content_type_id = ti.id\n+                WHERE p.id = %s\n+            \"\"\", (target_post_id,))\n+            result = cursor.fetchone()\n+            if result:\n+                content_type_name = result.get('content_type_name')\n+\n+        post_derived_theme = None\n+        post_context = None\n+        if post_type == 'themed' and post.get('id'):\n+            theme_desc = (post.get('summary') or post.get('subtitle') or '')\n+            post_derived_theme = {\n+                'post_id': post_id,\n+                'theme_title': post.get('title'),\n+                'theme_description': theme_desc,\n+                'priority': 'normal',\n+            }\n+            post_context = {\n+                'post_id': post_id,\n+                'title': post.get('title') or '',\n+                'summary': post.get('summary') or '',\n+                'subtitle': post.get('subtitle') or '',\n+            }\n+\n+        return render_template(\n+            'planning/calendar/ideas.html',\n+            post_id=post_id,\n+            post=post,\n+            post_type=post_type,\n+            post_title=post.get('title'),\n+            post_status=post.get('status'),\n+            post_created=post.get('created_at'),\n+            post_updated=post.get('updated_at'),\n+            content_type_name=content_type_name,\n+            post_derived_theme=post_derived_theme,\n+            post_context=post_context,\n+            year=year,\n+            week_number=week_number,\n+            blueprint_name='planning',\n+            mode='post-based',\n+            page_mode=page_mode,\n+        )\n+    except Exception as e:\n+        logger.error(f\"Error in _render_ideas_page({page_mode}) for post {post_id}: {e}\")\n+        return ideas_func(post_id)\n+```\n+\n+### 1.2 Legacy route preserved\n+\n+`/planning/posts/<id>/calendar/ideas` now redirects (302) to the Generate page:\n+\n+```python\n+@bp.route('/posts/<int:post_id>/calendar/ideas')\n+def planning_calendar_ideas(post_id):\n+    \"\"\"Legacy ideas calendar entry; redirect to generate page for post-based flow.\"\"\"\n+    from flask import redirect, url_for, request\n+\n+    year = request.args.get('year', type=int)\n+    week = request.args.get('week', type=int)\n+    return redirect(url_for('planning.planning_calendar_ideas_generate', post_id=post_id, year=year, week=week))\n+```\n+\n+HTTP checks (post 729):\n+\n+```bash\n+curl -s -o /dev/null -w \"%{http_code} %{url_effective}\\n\" \"http://localhost:5000/planning/posts/729/calendar/ideas/generate\"\n+# 200 http://localhost:5000/planning/posts/729/calendar/ideas/generate\n+\n+curl -s -o /dev/null -w \"%{http_code} %{url_effective}\\n\" \"http://localhost:5000/planning/posts/729/calendar/ideas/curate\"\n+# 200 http://localhost:5000/planning/posts/729/calendar/ideas/curate\n+\n+curl -s -o /dev/null -w \"%{http_code} %{url_effective}\\n\" \"http://localhost:5000/planning/posts/729/calendar/ideas\"\n+# 302 http://localhost:5000/planning/posts/729/calendar/ideas\n+```\n+\n+Both new routes return **200**; the legacy route returns **302** (redirect-only), as required.\n+\n+---\n+\n+## 2. Page Responsibilities (Generate vs Curate)\n+\n+Template: `templates/planning/calendar/ideas.html`\n+\n+Page mode is driven by `page_mode`:\n+\n+```jinja2\n+{% set page_mode = page_mode|default('curate') %}\n+```\n+\n+### 2.1 Generate Page (RUN_PAGE)\n+\n+Rendered via `/planning/posts/<id>/calendar/ideas/generate` with `page_mode=\"generate\"`.\n+\n+- **Contains:**\n+  - Header / theme display (via shared header strip).\n+  - **Generate Idea Set** button:\n+\n+    ```jinja2\n+    {% if page_mode == 'generate' %}\n+    <div class=\"metadata-field\" style=\"margin-bottom: 1rem;\">\n+        <button type=\"button\" \n+                id=\"generate-idea-set-btn\" \n+                class=\"btn btn-primary\"\n+                style=\"display: inline-flex; align-items: center; gap: 0.5rem;\"\n+                onclick=\"runGenerateIdeaSet()\">\n+            <i class=\"fas fa-magic\"></i> Generate Idea Set\n+        </button>\n+        <span id=\"generate-idea-set-status\" style=\"margin-left: 0.75rem; color: #94a3b8; font-size: 0.875rem;\"></span>\n+    </div>\n+    {% endif %}\n+    ```\n+\n+  - **Artefact summary** (total / selected / categories):\n+\n+    ```jinja2\n+    <small style=\"color: #94a3b8; font-size: 0.75rem;\">\n+        Total: <span id=\"required-ideas-total-count\">0</span>,\n+        Selected: <span id=\"required-ideas-selected-count\">0</span> (min 10),\n+        Categories: <span id=\"required-ideas-category-count\">0</span> (min 3)\n+    </small>\n+    ```\n+\n+  - A short summary note explaining that detailed editing happens on the Curate page.\n+  - **Next button:**\n+\n+    ```jinja2\n+    {% if page_mode == 'generate' %}\n+    <div class=\"metadata-field\" style=\"margin-top: 0.5rem;\">\n+        <a href=\"{{ url_for('planning.planning_calendar_ideas_curate', post_id=post_id) }}\" class=\"btn btn-primary\">\n+            Next: Curate Ideas\n+        </a>\n+    </div>\n+    {% endif %}\n+    ```\n+\n+- **Does NOT contain:**\n+  - Required ideas editable table (`required-ideas-list`) or the `Add Idea` / `Select all` / `Select none` editor controls (they are wrapped in `{% if page_mode == 'curate' %}` and thus omitted on generate).\n+\n+### 2.2 Curate Page (EDIT_PAGE)\n+\n+Rendered via `/planning/posts/<id>/calendar/ideas/curate` with `page_mode=\"curate\"` (default).\n+\n+- **Contains:**\n+  - Full **Required Ideas editor** including list, selection toggles, category dropdown per row, and delete/add operations.\n+  - The “Select all”, “Select none”, and “Add Idea” buttons (only when `page_mode == 'curate'`).\n+  - The existing subtitle/metadata editor and LLM module.\n+  - **Next button:**\n+\n+    ```jinja2\n+    {% elif page_mode == 'curate' %}\n+    <div class=\"metadata-field\" style=\"margin-top: 0.5rem;\">\n+        <a href=\"/planning/posts/{{ post_id }}/calendar/structure\" class=\"btn btn-primary\">\n+            Next: Structure → Cluster\n+        </a>\n+    </div>\n+    {% endif %}\n+    ```\n+\n+- **Does NOT contain:**\n+  - A visible Generate button section — that block is guarded by `{% if page_mode == 'generate' %}` and therefore hidden on the Curate page.\n+\n+---\n+\n+## 3. Registry / nav_url updates\n+\n+In `blueprints/posts.py`, `_get_pipeline_nav` now routes ideas substages to their dedicated pages:\n+\n+```python\n+def _get_pipeline_nav(stage: str, substage: str, post_id: int):\n+    \"\"\"Hard-coded canonical nav_url mappings for pipeline navigation, with nav_exists flag.\"\"\"\n+    # metadata.* → /calendar/metadata (page may not exist yet)\n+    if stage == \"metadata\":\n+        return f\"/planning/posts/{post_id}/calendar/metadata\", False\n+    if stage == \"ideas\":\n+        # N-PAGE-IMPL-1: split Ideas into generate vs curate pages\n+        if substage == \"generate_idea_set\":\n+            return f\"/planning/posts/{post_id}/calendar/ideas/generate\", True\n+        if substage == \"curate_ideas\":\n+            return f\"/planning/posts/{post_id}/calendar/ideas/curate\", True\n+        return f\"/planning/posts/{post_id}/calendar/ideas\", True\n+    if stage == \"structure\":\n+        return f\"/planning/posts/{post_id}/calendar/structure\", False\n+    ...\n+```\n+\n+### 3.1 Jump list / pipeline-state nav_url\n+\n+After the change, `GET /api/posts/729/pipeline-state` shows the updated URLs:\n+\n+```bash\n+curl -s http://localhost:5000/api/posts/729/pipeline-state | jq\n+```\n+\n+Excerpt:\n+\n+```json\n+{\n+  \"post_id\": 729,\n+  \"post_type\": \"themed\",\n+  \"workflow_stage\": \"structure\",\n+  \"substages\": [\n+    {\n+      \"stage\": \"ideas\",\n+      \"substage\": \"generate_idea_set\",\n+      \"label\": \"Generate Idea Set\",\n+      \"nav_exists\": true,\n+      \"nav_url\": \"/planning/posts/729/calendar/ideas/generate\",\n+      ...\n+    },\n+    {\n+      \"stage\": \"ideas\",\n+      \"substage\": \"curate_ideas\",\n+      \"label\": \"Curate ideas\",\n+      \"nav_exists\": true,\n+      \"nav_url\": \"/planning/posts/729/calendar/ideas/curate\",\n+      ...\n+    },\n+    ...\n+  ]\n+}\n+```\n+\n+The header Jump list uses `pipeline-state` for navigation, so it now points to the correct dedicated pages.\n+\n+---\n+\n+## 4. Pipeline-state Validation\n+\n+Current state for post 729 (after implementation; DB already contains generated + curated ideas, but cluster not yet complete):\n+\n+```bash\n+curl -s http://localhost:5000/api/posts/729/pipeline-state | jq\n+```\n+\n+Excerpt:\n+\n+```json\n+{\n+  \"current\": {\n+    \"stage\": \"structure\",\n+    \"substage\": \"cluster_into_sections\"\n+  },\n+  \"next\": {\n+    \"stage\": \"structure\",\n+    \"substage\": \"edit_section_plan\"\n+  },\n+  \"post_id\": 729,\n+  \"post_type\": \"themed\",\n+  \"reasons_blocked\": {\n+    \"structure.cluster_into_sections\": \"Need at least 10 selected ideas (have 29); at least 3 categories (have 5); and at least 6 sections (have 5).\"\n+  },\n+  \"substages\": [\n+    { \"stage\": \"ideas\", \"substage\": \"generate_idea_set\", \"label\": \"Generate Idea Set\", \"is_complete\": true, ... },\n+    { \"stage\": \"ideas\", \"substage\": \"curate_ideas\", \"label\": \"Curate ideas\", \"is_complete\": true, ... },\n+    { \"stage\": \"structure\", \"substage\": \"cluster_into_sections\", \"label\": \"Cluster into Sections\", \"is_complete\": false, ... },\n+    ...\n+  ]\n+}\n+```\n+\n+Interpretation:\n+- Idea set and curation are already complete for this post, so **current** is `structure.cluster_into_sections` and **next** is `structure.edit_section_plan`, as before.\n+- The page split did **not** alter gating or completion logic; it only changed which URLs are used to perform each step.\n+\n+Expected behaviour in other data states (logic unchanged, so still holds):\n+- If **no ideas** exist for a themed post: `current = ideas.generate_idea_set`, `next = ideas.curate_ideas`.\n+- If ideas exist but curate thresholds are not met: `current = ideas.curate_ideas`, `next = structure.cluster_into_sections`.\n+- If curate thresholds are satisfied: first incomplete substage moves to `structure.cluster_into_sections` (as seen for 729 once sections are insufficient).\n+\n+---\n+\n+## 5. Gating Integrity\n+\n+No changes were made to gating logic; the page split only reuses existing artefact rules:\n+\n+- **Generate page completion:** `post_required_idea.total >= 30` and categories >= 4 (as per earlier Phase 3.2).\n+- **Curate page completion:** `selected >= 10` AND `selected_categories >= 3` (as enforced by pipeline-state / pipeline_gates).\n+\n+The new pages simply provide dedicated surfaces for each operation:\n+- RUN_PAGE (`/ideas/generate`) runs the LLM idea generator and triggers the existing execute-substage handler.\n+- EDIT_PAGE (`/ideas/curate`) surfaces the existing required-ideas editor used to meet the curation thresholds.\n+\n+---\n+\n+## 6. Acceptance Checklist\n+\n+- **Both new routes return HTTP 200**\n+  - `/planning/posts/729/calendar/ideas/generate` → `200`\n+  - `/planning/posts/729/calendar/ideas/curate` → `200`\n+\n+- **Old route behaviour**\n+  - `/planning/posts/729/calendar/ideas` → `302` (redirects to the Generate page; no deletion of the legacy entry point).\n+\n+- **Jump list URLs**\n+  - `ideas.generate_idea_set` Jump entry now points to `/planning/posts/<id>/calendar/ideas/generate`.\n+  - `ideas.curate_ideas` Jump entry now points to `/planning/posts/<id>/calendar/ideas/curate`.\n+\n+- **pipeline-state.current behaviour**\n+  - Verified `current`/`next` for 729 remain structure-focused (ideas already complete), demonstrating that gating logic is unchanged and still anchored to artefacts rather than URLs.\n+\n+- **No duplicate Generate button**\n+  - The Generate button block is wrapped in `{% if page_mode == 'generate' %}` and thus only appears on `/ideas/generate`.\n+  - The Curate page (`/ideas/curate`) shows only the editing UI and counters.\n+\n+- **No editing UI on Generate page**\n+  - The required-ideas editable table (`required-ideas-list`) and its controls (`Select all`, `Select none`, `Add Idea`) are wrapped in `{% if page_mode == 'curate' %}` and therefore absent on `/ideas/generate`.\n+  - Generate page shows only the artefact summary (total/selected/categories) and the Next-to-Curate button.\n+\n+Overall, the Ideas stage is now split into a clean RUN_PAGE and EDIT_PAGE pair, aligned to the canonical themed pipeline, while preserving the legacy `/calendar/ideas` route as a redirect and leaving gating logic untouched.\n+\n*** End Patch***"/>
