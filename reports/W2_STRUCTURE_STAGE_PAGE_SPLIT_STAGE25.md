# W2 — Structure Stage Page Split (Stage 25)\n+\n+Refactor Structure stage into two contract-pure pages:\n+- RUN_PAGE → `/planning/posts/<id>/calendar/structure/cluster`\n+- EDIT_PAGE → `/planning/posts/<id>/calendar/structure/edit`\n+\n+Preserve artefact-driven state logic. No completion logic changes; no deletions of existing logic.\n+\n+---\n+\n+## 1. Routes\n+\n+**New routes (no deletions)** — in `blueprints/planning.py`:\n+\n+```python\n+@bp.route(\"/posts/<int:post_id>/calendar/structure/cluster\")\n+def planning_calendar_structure_cluster(post_id):\n+    \"\"\"Structure Cluster page (RUN_PAGE: structure.cluster_into_sections).\"\"\"\n+    return _render_structure_page(post_id, page_mode=\"cluster\")\n+\n+\n+@bp.route(\"/posts/<int:post_id>/calendar/structure/edit\")\n+def planning_calendar_structure_edit(post_id):\n+    \"\"\"Structure Edit page (EDIT_PAGE: structure.edit_section_plan).\"\"\"\n+    return _render_structure_page(post_id, page_mode=\"edit\")\n+```\n+\n+**Legacy route preserved as redirect-only**:\n+\n+```python\n+@bp.route(\"/posts/<int:post_id>/calendar/structure\")\n+def planning_calendar_structure(post_id):\n+    \"\"\"Legacy structure calendar entry; redirect to cluster page for post-based flow.\"\"\"\n+    from flask import redirect, url_for, request\n+\n+    year = request.args.get(\"year\", type=int)\n+    week = request.args.get(\"week\", type=int)\n+    return redirect(\n+        url_for(\"planning.planning_calendar_structure_cluster\", post_id=post_id, year=year, week=week)\n+    )\n+```\n+\n+### Route tests (curl)\n+\n+_Note: at the time of this report, the dev server on `localhost:5000` was not running, so curl returned `000` (connection failed). Once the server is running, the expected statuses are:_\n+\n+```bash\n+curl -s -o /dev/null -w \"%{http_code} %{url_effective}\\n\" \"http://localhost:5000/planning/posts/729/calendar/structure/cluster\"\n+# expected: 200 http://localhost:5000/planning/posts/729/calendar/structure/cluster\n+\n+curl -s -o /dev/null -w \"%{http_code} %{url_effective}\\n\" \"http://localhost:5000/planning/posts/729/calendar/structure/edit\"\n+# expected: 200 http://localhost:5000/planning/posts/729/calendar/structure/edit\n+\n+curl -s -o /dev/null -w \"%{http_code} %{url_effective}\\n\" \"http://localhost:5000/planning/posts/729/calendar/structure\"\n+# expected: 302 http://localhost:5000/planning/posts/729/calendar/structure\n+```\n+\n+Routing logic itself is defined; HTTP verification must be re-run locally once the Flask app is running.\n+\n+---\n+\n+## 2. Shared renderer: `_render_structure_page`\n+\n+Implemented in `blueprints/planning.py`:\n+\n+```python\n+def _render_structure_page(post_id: int, page_mode: str):\n+    \"\"\"\n+    Shared renderer for post-based Structure pages.\n+\n+    page_mode:\n+      - \"cluster\" for RUN_PAGE (/calendar/structure/cluster)\n+      - \"edit\" for EDIT_PAGE (/calendar/structure/edit)\n+    \"\"\"\n+    try:\n+        from flask import request, redirect, url_for\n+        from datetime import datetime\n+        from utils.taxonomy_helpers import get_post_type\n+\n+        post_type = get_post_type(post_id)\n+        # Keep type-based redirects consistent with Ideas page\n+        if post_type == \"recipe\":\n+            return redirect(url_for(\"authoring.authoring_sections_drafting\", post_id=post_id))\n+        elif post_type == \"generated\":\n+            return redirect(url_for(\"planning.planning_calendar_taxonomy\", post_id=post_id))\n+\n+        # Week context (optional; for navigation consistency)\n+        url_year = request.args.get(\"year\", type=int)\n+        url_week = request.args.get(\"week\", type=int)\n+        if url_year and url_week:\n+            year = url_year\n+            week_number = url_week\n+        else:\n+            now = datetime.now()\n+            year = now.year\n+            week_number = now.isocalendar()[1]\n+\n+        target_post_id = post_id\n+        content_type_name = None\n+        with db_manager.get_cursor() as cursor:\n+            cursor.execute(\n+                \"\"\"\n+                SELECT p.id, p.title, p.status, p.summary, p.subtitle, p.created_at, p.updated_at,\n+                       p.content_type_id\n+                FROM post p\n+                WHERE p.id = %s\n+                \"\"\",\n+                (target_post_id,),\n+            )\n+            post = cursor.fetchone()\n+\n+            if not post:\n+                return render_template(\n+                    \"planning/calendar/structure.html\",\n+                    post_id=post_id,\n+                    year=year,\n+                    week_number=week_number,\n+                    blueprint_name=\"planning\",\n+                    mode=\"post-based\",\n+                    page_mode=page_mode,\n+                    error=\"Post not found\",\n+                )\n+\n+            cursor.execute(\n+                \"\"\"\n+                SELECT ti.display_name as content_type_name\n+                FROM post p\n+                LEFT JOIN taxonomy_item ti ON p.content_type_id = ti.id\n+                WHERE p.id = %s\n+                \"\"\",\n+                (target_post_id,),\n+            )\n+            result = cursor.fetchone()\n+            if result:\n+                content_type_name = result.get(\"content_type_name\")\n+\n+        # Do not mutate artefacts; pass post context only\n+        post_derived_theme = None\n+        post_context = None\n+        if post_type == \"themed\" and post.get(\"id\"):\n+            theme_desc = (post.get(\"summary\") or post.get(\"subtitle\") or \"\")\n+            post_derived_theme = {\n+                \"post_id\": post_id,\n+                \"theme_title\": post.get(\"title\"),\n+                \"theme_description\": theme_desc,\n+                \"priority\": \"normal\",\n+            }\n+            post_context = {\n+                \"post_id\": post_id,\n+                \"title\": post.get(\"title\") or \"\",\n+                \"summary\": post.get(\"summary\") or \"\",\n+                \"subtitle\": post.get(\"subtitle\") or \"\",\n+            }\n+\n+        return render_template(\n+            \"planning/calendar/structure.html\",\n+            post_id=post_id,\n+            post=post,\n+            post_type=post_type,\n+            post_title=post.get(\"title\"),\n+            post_status=post.get(\"status\"),\n+            post_created=post.get(\"created_at\"),\n+            post_updated=post.get(\"updated_at\"),\n+            content_type_name=content_type_name,\n+            post_derived_theme=post_derived_theme,\n+            post_context=post_context,\n+            year=year,\n+            week_number=week_number,\n+            blueprint_name=\"planning\",\n+            mode=\"post-based\",\n+            page_mode=page_mode,\n+        )\n+    except Exception as e:\n+        logger.error(f\"Error in _render_structure_page({page_mode}) for post {post_id}: {e}\")\n+        # Fallback: render generic planning structure page if available\n+        return render_template(\"planning/structure.html\", post_id=post_id)\n+```\n+\n+This reuses the same DB-loading pattern as the Ideas renderer, performs no artefact writes, and injects `page_mode` into the new calendar Structure template.\n+\n+---\n+\n+## 3. Template Split (RUN vs EDIT)\n+\n+New template: `templates/planning/calendar/structure.html`.\n+\n+At the top:\n+\n+```jinja2\n+{% extends \"base.html\" %}\n+\n+{% set page_mode = page_mode|default('edit') %}\n+\n+{% block title %}Structure - Planning{% endblock %}\n+```\n+\n+### page_mode == \"cluster\" (RUN_PAGE)\n+\n+Rendered via `/planning/posts/<id>/calendar/structure/cluster`:\n+\n+- Shows a **Cluster Into Sections** button and a minimal sections count placeholder:\n+\n+```jinja2\n+{% if page_mode == 'cluster' %}\n+<div style=\"background:#0f172a; border:1px solid #334155; border-radius:8px; padding:1.25rem; margin-bottom:1.25rem;\">\n+    <h3>Cluster Into Sections</h3>\n+    <p>Run the clustering step to turn curated ideas into a structured set of sections.</p>\n+    <div>\n+        <div>Sections</div>\n+        <div id=\"structure-sections-count\">—</div>\n+    </div>\n+    <button type=\"button\"\n+            id=\"cluster-into-sections-btn\"\n+            class=\"btn btn-primary\">\n+        <i class=\"fas fa-layer-group\"></i> Cluster Into Sections\n+    </button>\n+</div>\n+<div style=\"margin-top:0.5rem;\">\n+    <a href=\"{{ url_for('planning.planning_calendar_structure_edit', post_id=post_id) }}\" class=\"btn btn-secondary\">\n+        Next: Edit Sections\n+    </a>\n+</div>\n+{% elif page_mode == 'edit' %}\n+...\n+{% endif %}\n+```\n+\n+- Contains **no section editing table or controls** — only the run button, summary placeholder, and a Next-to-Edit button.\n+\n+### page_mode == \"edit\" (EDIT_PAGE)\n+\n+Rendered via `/planning/posts/<id>/calendar/structure/edit`:\n+\n+- Embeds the existing **Sections panel** UI via iframe to reuse the full editing interface without duplicating logic:\n+\n+```jinja2\n+{% elif page_mode == 'edit' %}\n+<div style=\"margin-bottom:1.25rem;\">\n+    <p>Edit section titles, order, and content below. Changes are saved via the existing Sections panel.</p>\n+    <iframe src=\"/post-sections/?post_id={{ post_id }}\"\n+            style=\"width:100%; min-height:600px; border:none; border-radius:8px; overflow:hidden;\"\n+            title=\"Sections editor for post {{ post_id }}\"></iframe>\n+</div>\n+<div style=\"margin-top:0.5rem;\">\n+    <a href=\"/planning/posts/{{ post_id }}/calendar/titling\" class=\"btn btn-primary\">\n+        Next: Titling\n+    </a>\n+</div>\n+{% endif %}\n+```\n+\n+- Contains **no Cluster button**; only editing UI and the downstream Next button.\n+\n+No hidden DOM with removed controls is introduced; the embedded `/post-sections` panel remains unchanged and is only present on the Edit page.\n+\n+---\n+\n+## 4. Navigation Update (`_get_pipeline_nav`)\n+\n+In `blueprints/posts.py`, `_get_pipeline_nav` now maps Structure substages to the new URLs:\n+\n+```python\n+def _get_pipeline_nav(stage: str, substage: str, post_id: int):\n+    \"\"\"Hard-coded canonical nav_url mappings for pipeline navigation, with nav_exists flag.\"\"\"\n+    # metadata.* → /calendar/metadata (page may not exist yet)\n+    if stage == \"metadata\":\n+        return f\"/planning/posts/{post_id}/calendar/metadata\", False\n+    if stage == \"ideas\":\n+        ...\n+    if stage == \"structure\":\n+        # Stage 25: split Structure into cluster vs edit pages\n+        if substage == \"cluster_into_sections\":\n+            return f\"/planning/posts/{post_id}/calendar/structure/cluster\", True\n+        if substage == \"edit_section_plan\":\n+            return f\"/planning/posts/{post_id}/calendar/structure/edit\", True\n+        return f\"/planning/posts/{post_id}/calendar/structure\", False\n+```\n+\n+This means both `structure.cluster_into_sections` and `structure.edit_section_plan` now have `nav_exists = True` and point to their dedicated pages. Other stages are unchanged; completion logic and pipeline-state computation are untouched.\n+\n+Additionally, the Ideas page \"Next: Structure → Cluster\" link was updated to use the new route name instead of a hard-coded `/calendar/structure` URL:\n+\n+```jinja2\n+{% elif page_mode == 'curate' %}\n+<div class=\"metadata-field\" style=\"margin-top: 0.5rem;\">\n+    <a href=\"{{ url_for('planning.planning_calendar_structure_cluster', post_id=post_id) }}\" class=\"btn btn-primary\">\n+        Next: Structure → Cluster\n+    </a>\n+</div>\n+{% endif %}\n+```\n+\n+### Hardcoded link audit\n+\n+Scoped grep:\n+\n+```bash\n+grep -R \"/calendar/structure\" blueprints templates\n+```\n+\n+Output:\n+\n+```text\n+blueprints/posts.py:            return f\"/planning/posts/{post_id}/calendar/structure/cluster\", True\n+blueprints/posts.py:            return f\"/planning/posts/{post_id}/calendar/structure/edit\", True\n+blueprints/posts.py:        return f\"/planning/posts/{post_id}/calendar/structure\", False\n+blueprints/planning.py:      - \"cluster\" for RUN_PAGE (/calendar/structure/cluster)\n+blueprints/planning.py:      - \"edit\" for EDIT_PAGE (/calendar/structure/edit)\n+blueprints/planning.py:                    \"planning/calendar/structure.html\",\n+blueprints/planning.py:            \"planning/calendar/structure.html\",\n+blueprints/planning.py:@bp.route(\"/posts/<int:post_id>/calendar/structure/cluster\")\n+blueprints/planning.py:@bp.route(\"/posts/<int:post_id>/calendar/structure/edit\")\n+blueprints/planning.py:@bp.route(\"/posts/<int:post_id>/calendar/structure\")\n+```\n+\n+No template now hard-codes `/planning/posts/<id>/calendar/structure`; only the redirect handler and navigation builder refer to it.\n+\n+---\n+\n+## 5. Pipeline-state Check\n+\n+Command (when server is running):\n+\n+```bash\n+curl -s http://localhost:5000/api/posts/729/pipeline-state | jq\n+```\n+\n+Expected excerpt (structure entries only):\n+\n+```json\n+{\n+  \"post_id\": 729,\n+  \"post_type\": \"themed\",\n+  \"workflow_stage\": \"structure\",\n+  \"substages\": [\n+    ...,\n+    {\n+      \"stage\": \"structure\",\n+      \"substage\": \"cluster_into_sections\",\n+      \"label\": \"Cluster into Sections\",\n+      \"nav_exists\": true,\n+      \"nav_url\": \"/planning/posts/729/calendar/structure/cluster\",\n+      ...\n+    },\n+    {\n+      \"stage\": \"structure\",\n+      \"substage\": \"edit_section_plan\",\n+      \"label\": \"Edit section plan\",\n+      \"nav_exists\": true,\n+      \"nav_url\": \"/planning/posts/729/calendar/structure/edit\",\n+      ...\n+    },\n+    ...\n+  ]\n+}\n+```\n+\n+The **current** and **next** fields remain governed purely by artefacts (ideas/sections), and were not modified by this split. Visiting the new pages does not mutate state; completion thresholds for clustering and editing remain enforced via existing gating logic.\n+\n+---\n+\n+## 6. Hard Requirements Checklist\n+\n+1. **Artefacts determine completion.**\n+   - Unchanged; no gating or pipeline-state logic was touched. Completion for `structure.cluster_into_sections` and downstream remains DB-driven.\n+\n+2. **Visiting pages does not mutate state.**\n+   - `_render_structure_page` only reads from the DB and passes context to the template; no writes are performed.\n+\n+3. **RUN page contains no editing UI.**\n+   - `/calendar/structure/cluster` shows only a Cluster button, a sections summary placeholder, and a Next-to-Edit button. No table, in-place editors, or reorder/add/remove controls are present.\n+\n+4. **EDIT page contains no generation trigger.**\n+   - `/calendar/structure/edit` embeds the existing Sections panel via iframe and a Next-to-Titling button; no Cluster button appears on this page.\n+\n+5. **pipeline-state.current logic unchanged.**\n+   - No code in the pipeline-state computation was altered; only `nav_url` values were updated.\n+\n+6. **No hidden DOM with active JS bound to removed controls.**\n+   - The edit UI is only present on the Edit page; the RUN page does not include the sections editor DOM. The embedded `/post-sections` UI remains intact and continues to manage its own JS within its iframe or route.\n+\n+---\n+\n+## 7. Summary\n+\n+- New routes:\n+  - `/planning/posts/<id>/calendar/structure/cluster` (Structure RUN_PAGE)\n+  - `/planning/posts/<id>/calendar/structure/edit` (Structure EDIT_PAGE)\n+  - `/planning/posts/<id>/calendar/structure` (redirect to cluster)\n+- Shared `_render_structure_page` function reuses existing DB-loading patterns and injects `page_mode`.\n+- New template `planning/calendar/structure.html` splits UI cleanly between RUN and EDIT without altering business logic.\n+- `_get_pipeline_nav` now points `structure.cluster_into_sections` and `structure.edit_section_plan` at the correct dedicated URLs with `nav_exists = True`.\n+\n+Commit: to be recorded after all changes are staged and verified locally.\n+\n*** End Patch
